# Storage Providers

Fangorn is a somewhat storage agnostic solution. It does not care if you use pinata or IPFS directly, with the sole caveat that content is addressed using CIDs instead of URLs.

Fangorn currently supports [Pinata](https://pinata.cloud/).