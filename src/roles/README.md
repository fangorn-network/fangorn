# Roles

Namespaced roles in Fangorn:

- Schema Owner — registerAgent, register, get
- Publisher — upload, add, remove, commit
- Consumer — register, settle, decrypt

## Usage

Init a Fangorn client and use it to access each namespace.

``` ts
// TODO
```