# Gadgets

A gadget represents a logical condition with public inputs that can be used to gate access to data.
The gaddgets framework is extensible, allowing developers to build their own gadgets for use.
